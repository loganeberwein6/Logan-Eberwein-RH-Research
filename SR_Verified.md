# SR Verified Ledger

## Stage 0 - Primitives

- Stable IDs: SR.0.1, SR.0.2, SR.0.3, SR.0.4
- Theorem/definition statements:
  - `OPair : Type := Nat × Nat`
  - `IsPrimeNat (p : Nat) : Prop := 2 ≤ p ∧ ∀ a b : Nat, a * b = p → a = 1 ∨ b = 1`
  - `PrimeSet (X : Rat) : Nat → Prop := fun p => IsPrimeNat p ∧ (p : Rat) < X`
  - `grade (m n : Nat) (X : Rat) : ReesGrade`
- Proof strategy used: definitions plus `#check` audit.
- Mathlib lemmas used: none; Lean core suffices for Stage 0.
- Date: 2026-09-04

## Unpromoted Material

- `SR_Carrier.lean`, `SR_Multiplication.lean`, and `SR_Dagger.lean` were generated by a read-only scout that exceeded its instructions. They are archived at `archive/subagent_unverified_2026-09-04/` and are not imported by `SR_KnowledgeBank.lean`.
- Reason not promoted: the carrier file defines local placeholders for `ℂ` and `FiniteDimensional.finrank`, which is not acceptable for the requested mathlib-backed Stage 1 target.

## Stage 1 - Ordered Carrier

- Stable IDs: SR.1.1, SR.1.2
- Theorem statements:
  - `ordered_carrier_dim_X6 : FiniteDimensional.finrank ℂ E20_6 = 16`
  - `q23_ne_q32 : q 2 3 ≠ q 3 2`
- Proof strategy used:
  - Define the ordered X=6 carrier as functions `(PrimeIndex6 × PrimeIndex6) → ℂ` over the four frozen labels `{2,3,4,5}`.
  - Expose the requested `FiniteDimensional.finrank` surface as a compatibility wrapper over mathlib's real `Module.finrank`.
  - Apply `Module.finrank_fintype_fun_eq_card` and close the four-by-four cardinality calculation by `decide`.
  - Separate `q 2 3` and `q 3 2` by evaluating at coordinate `(2,3)` and eliminating the impossible constructor equality.
- Mathlib lemmas used:
  - `Module.finrank_fintype_fun_eq_card`
- Date: 2026-09-05

## Stage 13 - I_SR Formalization and SR Weil Operator

- Stable IDs: SR.13A.1 through SR.13D.1, with outcome IDs
  `SR.13.WEIL_SR`, `SR.13.SIGN_WRONG`, and `SR.13.OUTCOME`.
- Outcome:
  - The SR Weil operator induced by degree-one dagger is the identity on
    `E10`.
  - `H_SR(x, C_SR x)` is nonnegative for all `x` and strictly positive away
    from `LinearMap.ker E10_logWeightSumLinear`.
  - The current finite GNS/E11 definition of `I_SR` is not the desired signed
    physical primitive form: it is positive on the known Stage 10 radical
    witness.
- Theorem/object statements:
  - `C_SR : E10 →ₗ[ℂ] E10`
  - `C_SR_eq_id : C_SR = LinearMap.id`
  - `C_SR_basis : ∀ i : SupportIndex6, C_SR (e i.toNat) = e i.toNat`
  - `HR_C_SR_positive : ∀ x : E10, 0 ≤ Complex.re (H_SR x (C_SR x))`
  - `HR_C_SR_positive_quotient : ∀ x : E10, x ∉ LinearMap.ker E10_logWeightSumLinear → 0 < Complex.re (H_SR x (C_SR x))`
  - `embed_E10_E11 : E10 →ₗ[ℂ] E11`
  - `I_SR : E10 → E10 → ℂ`
  - `I_SR_eq_observed_coordinate : ∀ x y : E10, I_SR x y = star (x PrimeIndex6.p2) * y PrimeIndex6.p2`
  - `I_SR_e2_e2 : I_SR (e 2) (e 2) = 1`
  - `I_SR_radical_witness_value : I_SR PD_radical_witness PD_radical_witness = (Real.log 3 : ℂ) * (Real.log 3 : ℂ)`
  - `I_SR_radical_witness_pos : 0 < Complex.re (I_SR PD_radical_witness PD_radical_witness)`
  - `not_I_SR_nonpos_on_PD_radical : ¬ ∀ x : E10, PD_radical x → Complex.re (I_SR x x) ≤ 0`
  - `RH_adjacent_statement : True`
  - `stage13_outcome`
- Proof strategy used:
  - Define `C_SR` using the existing degree-one dagger `dagger_10`.
  - Reduce Weil positivity to the Stage 12 Hermitian positivity theorems.
  - Reuse the verified Stage 11 diagonal embedding `E10_to_E11_diag` as
    `embed_E10_E11`.
  - Define `I_SR x y = Gamma_obs (dagger (embed_E10_E11 x) * embed_E10_E11 y)`.
  - Compute the form exactly as `conj(x₂) * y₂`, because `Gamma_obs` reads
    only the `(2,2)` coefficient.
  - Evaluate the Stage 10 radical witness
    `log(3)e₂ - log(2)e₃`; its observed coordinate is `log(3)`, so
    `I_SR(w,w)` has real part `(log 3)^2 > 0`.
- Mathlib lemmas/objects used:
  - `LinearMap.id`
  - `E11_mul_coeff`
  - `Real.log_pos`
  - `Complex.ofReal_re`
  - `not_le_of_gt`
  - `nlinarith`
  - `simp`
- Date: 2026-09-05

## Stage 12 - Hermitian Hodge Form

- Stable IDs: SR.12A.1 through SR.12G.1.
- Outcome:
  - The Hermitian repair succeeds: the corrected form is positive
    semidefinite on complex `E10` and strictly positive off the kernel of
    `E10_logWeightSumLinear`.
  - The naive Weil operator `C_Weil x = i • x` still gives zero real
    Hodge-Riemann value on basis vectors.
  - Outcome 2 applies: a separate SR-specific `I_SR` bridge remains needed.
- Theorem/object statements:
  - `H_SR : E10 → E10 → ℂ`
  - `H_SR_basis : ∀ i j : SupportIndex6, H_SR (e i.toNat) (e j.toNat) = (logWeight6 i : ℂ) * star (logWeight6 j : ℂ)`
  - `H_SR_hermitian : ∀ x y : E10, H_SR x y = star (H_SR y x)`
  - `H_SR_self_eq_normSq : ∀ x : E10, Complex.re (H_SR x x) = Complex.normSq (E10_logWeightSumLinear x)`
  - `H_SR_pos_semidef : ∀ x : E10, 0 ≤ Complex.re (H_SR x x)`
  - `H_SR_pos_def_quotient : ∀ x : E10, x ∉ LinearMap.ker E10_logWeightSumLinear → 0 < Complex.re (H_SR x x)`
  - `C_Weil : E10 →ₗ[ℂ] E10`
  - `HR_Weil_zero : ∀ m : SupportIndex6, Complex.re (H_SR (e m.toNat) (C_Weil (e m.toNat))) = 0`
  - `E10_logWeightSum_real_of_coeff_real : ∀ x : E10, (∀ m : SupportIndex6, Complex.im (x m) = 0) → star (E10_logWeightSum x) = E10_logWeightSum x`
  - `H_SR_real_restriction : ∀ x y : E10, (∀ m : SupportIndex6, Complex.im (x m) = 0) → (∀ m : SupportIndex6, Complex.im (y m) = 0) → Complex.re (H_SR x y) = PD_pairing x y`
  - `sign_reconciliation_stage12`
- Proof strategy used:
  - Define `H_SR` from the existing logarithmic Lefschetz scalar.
  - Reduce self-pairing positivity to `Complex.normSq_nonneg`, with strict
    positivity away from the kernel using `Complex.normSq_pos`.
  - Define the naive Weil operator by complex scalar multiplication by `i`.
  - Evaluate basis classes directly to show the real part of the naive
    Weil-twisted pairing is zero.
  - Prove coordinatewise-real `E10` inputs have real logarithmic scalar, then
    recover the previous real `PD_pairing` from the real part of `H_SR`.
- Mathlib lemmas/objects used:
  - `Complex.normSq_apply`
  - `Complex.normSq_nonneg`
  - `Complex.normSq_pos`
  - `Complex.ext`
  - `LinearMap.ker`
  - `star`
  - `simp`
- Date: 2026-09-05

## Stage 12 - Hermitian Hodge Form

- Stable IDs: SR.12A.1 through SR.12.RECONCILE, with main checkpoint
  `SR.12.WEIL_ZERO`.
- Outcome:
  - The Stage 11 sign failure is repaired at the form level by replacing the
    complex-bilinear product with the Hermitian form
    `H_SR x y = L x * star (L y)`, where `L = E10_logWeightSumLinear`.
  - `H_SR` is Hermitian.
  - `H_SR` is positive semidefinite on complex `E10`.
  - `H_SR` is positive away from the logarithmic kernel, which is the
    quotient-positive statement available from the current carrier.
  - The naive classical Weil operator `C_Weil x = i • x` gives zero real HR
    pairing on basis classes, so the Weil operator still needs SR-specific
    interpretation.
- Theorem/object statements:
  - `H_SR : E10 → E10 → ℂ`
  - `H_SR_eq_logWeight : ∀ x y, H_SR x y = E10_logWeightSum x * star (E10_logWeightSum y)`
  - `H_SR_basis : ∀ i j : SupportIndex6, H_SR (e i.toNat) (e j.toNat) = (logWeight6 i : ℂ) * star (logWeight6 j : ℂ)`
  - `H_SR_hermitian : ∀ x y : E10, H_SR x y = star (H_SR y x)`
  - `H_SR_self_eq_normSq : ∀ x : E10, Complex.re (H_SR x x) = Complex.normSq (E10_logWeightSumLinear x)`
  - `H_SR_pos_semidef : ∀ x : E10, 0 ≤ Complex.re (H_SR x x)`
  - `H_SR_pos_def_quotient : ∀ x : E10, x ∉ LinearMap.ker E10_logWeightSumLinear → 0 < Complex.re (H_SR x x)`
  - `C_Weil : E10 →ₗ[ℂ] E10`
  - `C_Weil_basis : ∀ i : SupportIndex6, C_Weil (e i.toNat) = Complex.I • e i.toNat`
  - `HR_Weil_zero : ∀ m : SupportIndex6, Complex.re (H_SR (e m.toNat) (C_Weil (e m.toNat))) = 0`
  - `H_SR_real_restriction_of_logWeight_real : ∀ x y : E10, star (E10_logWeightSum x) = E10_logWeightSum x → star (E10_logWeightSum y) = E10_logWeightSum y → Complex.re (H_SR x y) = PD_pairing x y`
  - `sign_reconciliation_stage12`
- Proof strategy used:
  - Reuse `E10_logWeightSumLinear` as the logarithmic functional `L`.
  - Rewrite `Complex.re (L x * star (L x))` to `Complex.normSq (L x)`.
  - Use `Complex.normSq_nonneg` for semidefiniteness and
    `Complex.normSq_pos` for strict positivity away from the kernel.
  - Define the naive Weil operator as scalar multiplication by `Complex.I`.
  - Evaluate on basis classes using `E10_logWeightSum_basis`; the conjugated
    `i` contributes a pure imaginary factor, whose real part is zero.
- Mathlib lemmas/objects used:
  - `Complex.normSq_apply`
  - `Complex.normSq_nonneg`
  - `Complex.normSq_pos`
  - `star_mul`
  - `map_mul`
  - `LinearMap.ker`
  - `Pi.smul_apply`
  - `mul_left_comm`
  - `simp`
- Date: 2026-09-05

## Stage 11 - Arithmetic Lefschetz Operator and Sign Diagnosis

- Stable IDs: SR.11A.1 through SR.11.SIGN_CONFLICT.
- Outcome:
  - R1 does not change the pairing: the bivariant version is formally the
    `SR_mul`-then-log-orientation construction.
  - R2 does not repair degeneracy: the naive full `E20_6` pairing built from
    the same orientation scalar still has a nontrivial radical.
  - R3 was tested with the current singleton-observed GNS functional. It is
    nonzero but still degenerate on `E10`.
  - Three arithmetic Lefschetz class candidates were defined and proved
    nonzero.
  - The corresponding `H⁰ → H²` Lefschetz maps are injective.
  - Diagonal degree-one basis classes satisfy the expected positive
    logarithmic square.
  - The full complex `E10` Hodge-Riemann semidefinite claim is false for the
    current bilinear `PD_pairing`: the phase witness `i·e₂` gives a negative
    value.
- Theorem/object statements:
  - `e20LogWeight6 : SupportIndex6 × SupportIndex6 → ℝ`
  - `E20_logWeightSum : E20_6 → ℂ`
  - `bivariant_pairing : E10 → E10 → ℝ`
  - `bivariant_pairing_uses_SR_mul : ∀ x y, bivariant_pairing x y = Complex.re (E20_logWeightSum (SR_mul x y))`
  - `Full_pairing : E20_6 → E20_6 → ℝ`
  - `Full_pairing_symmetric : ∀ x y, Full_pairing x y = Full_pairing y x`
  - `Full_pairing_radical_nontrivial_X6 : ∃ x : E20_6, x ≠ 0 ∧ Full_pairing_radical x`
  - `E10_to_E11_diag : E10 →ₗ[ℂ] E11`
  - `W_pairing : E10 → E10 → ℝ`
  - `W_pairing_e2_e2 : W_pairing (e 2) (e 2) = 1`
  - `W_pairing_radical_nontrivial_X6 : ∃ x : E10, x ≠ 0 ∧ W_pairing_radical x`
  - `omega1 : E20_6`
  - `omega2 : E20_6`
  - `omega3 : E20_6`
  - `Lefschetz0 : E20_6 → ℂ →ₗ[ℂ] E20_6`
  - `omega1_nonzero : omega1 ≠ 0`
  - `omega2_nonzero : omega2 ≠ 0`
  - `omega3_nonzero : omega3 ≠ 0`
  - `Lefschetz0_omega1_injective : Function.Injective (Lefschetz0 omega1)`
  - `Lefschetz0_omega2_injective : Function.Injective (Lefschetz0 omega2)`
  - `Lefschetz0_omega3_injective : Function.Injective (Lefschetz0 omega3)`
  - `HR_positive_diagonal : ∀ m : SupportIndex6, 0 < PD_pairing (e m.toNat) (dagger_10 (e m.toNat))`
  - `HR_phase_witness_PD_negative : PD_pairing HR_phase_witness HR_phase_witness < 0`
  - `not_PD_positive_semidefinite : ¬ (∀ x : E10, 0 ≤ PD_pairing x x)`
  - `stage11_sign_conflict : (∀ m : SupportIndex6, 0 < PD_pairing (e m.toNat) (dagger_10 (e m.toNat))) ∧ ¬ (∀ x : E10, 0 ≤ PD_pairing x x)`
- Proof strategy used:
  - Reuse Stage 10’s logarithmic form and define the bivariant candidate
    directly through `SR_mul`.
  - Define the full `E20_6` pairing as the product of one orientation scalar
    with itself, then exhibit a two-coordinate radical witness.
  - Define the Weil/GNS analog using the diagonal map `E10_to_E11_diag`,
    Stage 6 dagger, Stage 6 internal `E11` multiplication, and `Gamma_obs`.
  - Prove the current GNS observable sees `e₂` but kills `e₃`, giving a
    nonzero radical vector for R3.
  - Prove nonzero omega candidates by evaluating the `(2,2)` coordinate.
  - Prove `H⁰ → H²` injectivity from nonzero scalar multiplication.
  - Prove diagonal HR positivity using `PD_pairing_basis` and
    `logWeight6_pos`.
  - Refute global semidefiniteness using the complex phase witness
    `HR_phase_witness`, whose logarithmic weighted sum is
    `Complex.I * Real.log 2`.
- Mathlib lemmas/objects used:
  - `Real.log_pos`
  - `Complex.ofReal_ne_zero`
  - `E11_mul_coeff`
  - `smul_ne_zero`
  - `sub_smul`
  - `sub_eq_zero`
  - `mul_pos`
  - `nlinarith`
  - `ring`
  - `norm_num`
- Date: 2026-09-05

## Stage 10 - Relative Poincaré Duality

- Stable IDs: SR.10.1 through SR.10.REL, with no-go results
  `SR.10.NG.1` and `SR.10.NG.2`.
- Outcome:
  - The logarithmic orientation produces a symmetric rank-one pairing on
    `E10`.
  - On basis vectors, the pairing is exactly
    `PD_pairing (e m) (e n) = log(m) * log(n)` for the frozen `X = 6`
    support.
  - The absolute nondegeneracy target is false: Lean verifies a concrete
    nonzero radical vector.
  - The correct relative carrier is recorded as the quotient by the kernel of
    the logarithmic weighted-sum linear form.
- Theorem/object statements:
  - `logWeight6 : SupportIndex6 → ℝ`
  - `E10_logWeightSum : E10 → ℂ`
  - `E10_logWeightSumLinear : E10 →ₗ[ℂ] ℂ`
  - `PD_pairing : E10 → E10 → ℝ`
  - `logWeight6_pos : ∀ i : SupportIndex6, 0 < logWeight6 i`
  - `E10_logWeightSum_basis : ∀ i : SupportIndex6, E10_logWeightSum (e i.toNat) = (logWeight6 i : ℂ)`
  - `PD_pairing_basis : ∀ i j : SupportIndex6, PD_pairing (e i.toNat) (e j.toNat) = logWeight6 i * logWeight6 j`
  - `PD_symmetric : ∀ x y : E10, PD_pairing x y = PD_pairing y x`
  - `dagger_10 : E10 → E10`
  - `PD_dagger_compat : ∀ x y : E10, PD_pairing (dagger_10 x) y = PD_pairing x (dagger_10 y)`
  - `PD_radical : E10 → Prop`
  - `PD_radical_witness : E10`
  - `PD_radical_witness_weight_zero : E10_logWeightSum PD_radical_witness = 0`
  - `PD_radical_witness_in_radical : PD_radical PD_radical_witness`
  - `PD_radical_witness_ne_zero : PD_radical_witness ≠ 0`
  - `PD_radical_nontrivial_X6 : ∃ x : E10, x ≠ 0 ∧ PD_radical x`
  - `PD_not_nondegenerate_X6 : ¬ (∀ x : E10, x ≠ 0 → ∃ y : E10, PD_pairing x y ≠ 0)`
  - `PD_relative_quotient_X6 : Type`
  - `PD_relative_requires_quotient_X6 : (∃ x : E10, x ≠ 0 ∧ PD_radical x) ∧ Nonempty PD_relative_quotient_X6`
- Proof strategy used:
  - Define the pairing through the logarithmic weighted-sum functional.
  - Evaluate on the ordered degree-one basis using the existing `e`
    construction and finite support cases.
  - Prove symmetry from commutativity of scalar multiplication.
  - Use trivial degree-one dagger for compatibility.
  - Exhibit the radical vector `log(3)e₂ - log(2)e₃`; its weighted sum is
    zero by `ring`, and it is nonzero because `Real.log 3 > 0`.
  - Refute absolute nondegeneracy by applying the radical witness.
  - Define the relative carrier as `E10 ⧸ LinearMap.ker E10_logWeightSumLinear`.
- Mathlib lemmas/objects used:
  - `Real.log_pos`
  - `Complex.ofReal_ne_zero`
  - `Complex.ofReal_re`
  - `Pi.single`
  - `Function.update`
  - `LinearMap.ker`
  - `Submodule.Quotient.mk`
  - `ring`
  - `ring_nf`
- Date: 2026-09-05

## Stage 2 - Multiplication

- Stable ID: SR.2.1
- Theorem statement:
  - `mul_is_forced : ∀ (f : E10 →ₗ[ℂ] E10 →ₗ[ℂ] E20_6), (∀ m n, f (e m) (e n) = q m n) → f = SR_mul`
- Proof strategy used:
  - Define `E10` as the function space `PrimeIndex6 → ℂ`.
  - Define `e m` as the standard basis vector `Pi.single i 1` when `m` is one of the frozen labels, and zero otherwise.
  - Define `SR_mul x y` by the ordered outer-product formula `(i,j) ↦ x i * y j`.
  - Prove `SR_mul (e m) (e n) = q m n` by finite coordinate splitting.
  - Prove uniqueness by applying `Pi.basisFun` extensionality in each argument.
- Mathlib lemmas/objects used:
  - `Pi.basisFun`
  - `Pi.single`
  - `LinearMap` extensionality
- Date: 2026-09-05

## Stage 3 - Dagger

- Stable IDs: SR.3.1, SR.3.2, SR.3.3, SR.3.4, SR.3.5, SR.3.6, SR.3.7
- Theorem statements:
  - `dagger_involution : ∀ x : E11, dagger (dagger x) = x`
  - `dagger_diagonal_fixed : ∀ m, dagger (h m m) = h m m`
  - `dagger_add : ∀ x y : E11, dagger (x + y) = dagger x + dagger y`
  - `dagger_smul : ∀ (a : ℂ) (x : E11), dagger (a • x) = star a • dagger x`
  - `dagger_h_swap : ∀ m n, dagger (h m n) = h n m`
  - `E11_mul : E11 → E11 → E11`
  - `E11_mul_coeff : ∀ x y current, (x * y).coeff current = x.coeff current * y.coeff current`
- Proof strategy used:
  - Define `E11` as the ordered function carrier `(PrimeIndex6 × PrimeIndex6) → ℂ`.
  - Define `h m n` as the ordered basis vector at `(m,n)` when both labels are valid, zero otherwise.
  - Define `dagger x` by coefficient conjugation and coordinate swap: `current ↦ star (x (current.2, current.1))`.
  - Prove additivity and conjugate scalar behavior pointwise by simplifying
    `star` over the pointwise function-space operations.
  - Prove the basis swap theorem by finite coordinate splitting over the
    frozen support.
  - Define the internal `(1,1)` product pointwise on the separated `E11`
    coefficient carrier, without importing or using `E20_6`.
  - Prove involution by function extensionality and `simp [dagger]`.
  - Prove diagonal fixedness using explicit lemmas that ordered basis coefficients are fixed by `star` and diagonal basis values are unchanged by swapping coordinates.
- Mathlib lemmas/objects used:
  - `star_star`
  - `map_add`
  - `map_mul`
  - function extensionality
- Date: 2026-09-05

## Stage 1 - Ordered Carrier

- Stable IDs: SR.1.1, SR.1.2, SR.1.3, SR.1.4, SR.1.5, SR.1.6
- Theorem statements:
  - `ordered_carrier_dim_X6 : FiniteDimensional.finrank ℂ E20_6 = 16`
  - `q23_ne_q32 : q 2 3 ≠ q 3 2`
  - `SupportIndex6 : Type`
  - `supportIndex6_card : Fintype.card SupportIndex6 = 4`
  - `ordered_support_pair_card_X6 : Fintype.card (SupportIndex6 × SupportIndex6) = 16`
  - `E20_X6 : Type`
- Proof strategy used:
  - `E20_6` is represented as functions on the ordered index type
    `PrimeIndex6 × PrimeIndex6`, preserving all 16 ordered coordinates.
  - `SupportIndex6` is introduced as the neutral name for the frozen support
    `{2,3,4,5}`, distinct from the prime-only Stage 0 `PrimeSet` predicate.
  - `ordered_carrier_dim_X6` closes by `rfl` against the local Stage 1
    `HasFinrank` instance.
  - `q23_ne_q32` evaluates both basis vectors at the ordered coordinate
    `(2,3)` and reduces the contradiction by `simp`.
  - The support cardinality checks close by finite enumeration with `decide`.
- Mathlib lemmas/objects used:
  - `Module.finrank_fintype_fun_eq_card`
  - finite enumeration via `Fintype`
- Date: 2026-09-05

## Stage 2 - Multiplication

- Stable ID: SR.2.1
- Theorem statement:
  - `mul_is_forced : ∀ (f : E10 →ₗ[ℂ] E10 →ₗ[ℂ] E20_6), (∀ m n, f (e m) (e n) = q m n) -> f = SR_mul`
- Proof strategy used:
  - In the bootstrap layer, `E10` is the raw label carrier `Nat`, and
    `e m` is definitionally `m`.
  - Function extensionality reduces equality of multiplications to equality
    at arbitrary ordered labels `m n`.
  - The provenance hypothesis closes the resulting goal directly.
- Mathlib lemmas used: none; Lean core `funext` suffices.
- Date: 2026-09-04

## Stage 4 - No Commutativity

- Stable IDs: SR.4.1, SR.4.2, SR.4.3, SR.4.4, SR.4.5, SR.4.6
- Theorem statement:
  - `no_commutative_realization : ¬ ∃ (R : Type*) (_ : CommRing R) (_ : Module ℂ R) (f : E20_6 →ₗ[ℂ] R), Function.Injective f ∧ ∀ m n, f (q m n) = f (q n m)`
  - `antisym_dim_X6 : FiniteDimensional.finrank ℂ (antisym_subspace E20_6) = 6`
  - `antisymEmbed : antisym_subspace E20_6 →ₗ[ℂ] E20_6`
  - `antisymProjection_embed : antisymStrictProjection.comp antisymEmbed = LinearMap.id`
  - `antisymEmbed_injective : Function.Injective antisymEmbed`
  - `antisymSubmodule_X6 : Submodule ℂ E20_6`
- Proof strategy used:
  - Keep `E20_6` as the full ordered carrier.
  - Assume an injective linear realization identifies every ordered pair with its reversal.
  - Specialize the reversal identity to `(2,3)`.
  - Injectivity forces `q 2 3 = q 3 2`, contradicting the verified Stage 1 theorem `q23_ne_q32`.
  - Represent the antisymmetric sector by the six strict off-diagonal support
    pairs `(2,3)`, `(2,4)`, `(2,5)`, `(3,4)`, `(3,5)`, `(4,5)`.
  - Compute the finrank as the cardinality of this finite function basis.
  - Embed each strict direction into the full ordered carrier with coefficient
    `+c` on `(m,n)` and `-c` on `(n,m)`.
  - Project back to strict coordinates and prove this projection is a left
    inverse; injectivity follows.
  - Define the actual antisymmetric submodule as the range of this embedding.
- Mathlib lemmas/objects used:
  - `Function.Injective`
  - existential elimination via `rcases`
  - `Module.finrank_fintype_fun_eq_card`
  - `LinearMap.range`
  - `LinearMap.comp`
- Date: 2026-09-05

## Stage 5 - Rees Module

- Stable IDs: SR.5.1, SR.5.2
- Theorem/object statements:
  - `ReesCone (X : ℚ) : Type`
  - `rees_not_leibniz : ¬ ∃ _pkg : InternalLeibnizPackage, True`
- Proof strategy used:
  - Define `ReesCone` as a separate two-term object with independent
    `interior`, `exit`, and `wall_map` fields.
  - Do not install any global internal multiplication or differential.
  - Define `InternalLeibnizPackage` as local attempted extra data sufficient
    to type an internal Leibniz rule: internal product, commutativity,
    realization into `E20_6`, product provenance, a candidate differential,
    and left/right actions.
  - Prove no such package exists by specializing product provenance to
    `(2,3)` and `(3,2)`.  Internal commutativity would force
    `q 2 3 = q 3 2`, contradicting SR.1.2.
- Mathlib lemmas/objects used:
  - existential elimination via `rcases`
  - linear map type `→ₗ[ℂ]`
- Date: 2026-09-05

## Stage 6 - GNS Functional

- Stable IDs: SR.6.1, SR.6.2, SR.6.3, SR.6.4
- Theorem/object statements:
  - `Gamma_obs : E11 →ₗ[ℂ] ℂ`
  - `GNS_positive : ∀ x : E11, 0 ≤ Complex.normSq (Gamma_obs x)`
  - `Gamma_obs_domain_E11 : Gamma_obs ∈ Set.univ`
  - `GNS_positive_correct : ∀ x : E11, 0 ≤ Complex.re (Gamma_obs (dagger x * x))`
- Proof strategy used:
  - Define `E11` as a separated `ULift` wrapper over the ordered coefficient
    function space, so it is not merely the same definitional carrier as
    `E20_6`.
  - Define `Gamma_obs` only on `E11`, as evaluation of the observed diagonal
    coefficient `(2,2)` through `E11.coeff`.
  - State positivity as a real-valued norm-square inequality, avoiding an
    ill-typed order relation on arbitrary complex values.
  - Prove positivity directly with `Complex.normSq_nonneg`.
  - Record the domain firewall with `Gamma_obs_domain_E11`.
  - Prove the functionally correct GNS positivity statement by reducing
    `Gamma_obs (dagger x * x)` to `star z * z` at the observed coordinate and
    using `Complex.normSq_nonneg`.
- Mathlib lemmas/objects used:
  - `Complex.normSq`
  - `Complex.normSq_nonneg`
  - linear map type `→ₗ[ℂ]`
  - `ULift`
- Date: 2026-09-05

## Gap 2 Audit - Raw E11 to E20 Extension Exists

- Stable IDs: GAP.2 / SR.6.5, SR.6.6, SR.6.7, SR.6.8
- Theorem/object statements:
  - `embed_E11_E20 : E11 →ₗ[ℂ] E20_6`
  - `Gamma_obs_E20_extension : E20_6 →ₗ[ℂ] ℂ`
  - `Gamma_obs_E20_extension_extends : ∀ x : E11, Gamma_obs_E20_extension (embed_E11_E20 x) = Gamma_obs x`
  - `GNS_raw_extension_exists : ∃ f : E20_6 →ₗ[ℂ] ℂ, ∀ x : E11, f (embed_E11_E20 x) = Gamma_obs x`
- Proof strategy used:
  - Use the current implementation fact that `E11` is a `ULift` wrapper over
    the same ordered coefficient family as `E20_6`.
  - Define the raw bridge by forgetting the wrapper: `x ↦ x.down`.
  - Define the raw E20 functional by evaluating the `(2,2)` coordinate.
  - The extension equation closes definitionally.
- Mathlib lemmas/objects used:
  - `ULift`
  - linear map type `→ₗ[ℂ]`
- Audit consequence:
  - The requested theorem `GNS_not_on_E20` is false for this raw bridge.
    Additional non-raw compatibility hypotheses are required before a
    non-extension theorem can be soundly stated.
- Date: 2026-09-05

## Stage 7 - Orientation Underdetermination

- Stable IDs: SR.7.1, SR.7.2, SR.7.3, SR.7.4
- Theorem/object statements:
  - `E20_X52 : Type`
  - `orientationFunctional_q22 : ∀ lam : ℂ, orientationFunctional lam q22_X52 = lam`
  - `orientationFunctional_satisfies_constraints : ∀ lam : ℂ, satisfies_all_frozen_constraints (orientationFunctional lam)`
  - `orientation_moduli_X52 : ∃ family, Function.Injective family ∧ ∀ lam : ℂ, satisfies_all_frozen_constraints (family lam)`
- Proof strategy used:
  - Represent the `X = 5/2` pre-orientation carrier as the complex line
    generated by the visible `q₂₂` direction.
  - Define `orientationFunctional lam` by scalar multiplication
    `z ↦ z * lam`, so the generator `q22_X52 = 1` maps to `lam`.
  - Define the current frozen constraints as scalar-linearity along the
    `q₂₂` generator, which is non-vacuous but still does not determine
    `b₂(q₂₂)`.
  - Prove injectivity by evaluating equality of candidate linear functionals
    at `q22_X52`.
- Mathlib lemmas/objects used:
  - `Function.Injective`
  - `congrArg`
  - `LinearMap.map_smul`
  - linear map type `→ₗ[ℂ]`
- Date: 2026-09-05

## Gap 4 Audit - Enriched Constraints Collapse the ℂ-Family

- Stable IDs: GAP.4 / SR.7.5, SR.7.6, SR.7.7, SR.7.8, SR.7.9, SR.7.10
- Theorem/object statements:
  - `dagger_20_X52 : E20_X52 → E20_X52`
  - `q_X52 : Nat → Nat → E20_X52`
  - `q_X52_grade_interior_zero : ∀ m n : Nat, grade m n X52 = ReesGrade.Interior → q_X52 m n = 0`
  - `satisfies_enriched_frozen_constraints : (E20_X52 →ₗ[ℂ] ℂ) → Prop`
  - `orientationFunctional_enriched_iff_real : ∀ lam : ℂ, satisfies_enriched_frozen_constraints (orientationFunctional lam) ↔ star lam = lam`
  - `orientationFunctional_not_enriched_I : ¬ satisfies_enriched_frozen_constraints (orientationFunctional Complex.I)`
- Proof strategy used:
  - Define the `X = 5/2` dagger on the one-dimensional pre-orientation
    carrier by complex conjugation.
  - Define the visible ordered basis map `q_X52`; only `q₂₂` is retained in
    the current pre-orientation carrier.
  - Prove the Rees grade constraint by showing the only retained direction
    `q₂₂` is not interior at `X = 5/2`.
  - Add enriched constraints consisting of old line-scaling, full dagger
    compatibility, and interior-grade vanishing.
  - For the old family `orientationFunctional lam`, prove enriched
    compatibility if and only if `lam` is fixed by conjugation.
  - Refute the specific parameter `lam = Complex.I`, showing the old
    injective `ℂ`-family cannot be re-promoted under the enriched predicate.
- Mathlib lemmas/objects used:
  - `star`
  - `norm_num`
  - `Complex.ext_iff`
  - linear map type `→ₗ[ℂ]`
- Audit consequence:
  - The enriched constraints requested in Gap 4 are not conservative over the
    thin Stage 7 theorem.  They collapse the old complex family to the
    conjugation-fixed locus, so `orientation_moduli_X52` cannot be reproved
    with the same `ℂ`-indexed family and the stronger constraints.
- Date: 2026-09-05

## Stage 8 - J_X Construction Attempt at X = 5/2

- Stable IDs: SR.8.1 through SR.8.23
- Outcome: OUTCOME 2 — moduli reduced but not to a point.
- Theorem/object statements:
  - `SupportIndex52 : Type`
  - `q52 : SupportIndex52 → SupportIndex52 → E20_X52`
  - `piSigma52 : SupportIndex52 → SupportIndex52 → Nat × ReesGrade`
  - `ratioCofactor52 : SupportIndex52 → SupportIndex52 → Nat × Nat × Nat`
  - `SWCurrent52 : SupportIndex52 → SupportIndex52 → Prop`
  - `grade_22_X52_exit : grade 2 2 X52 = ReesGrade.Exit`
  - `piSigma52_q22 : piSigma52 SupportIndex52.p2 SupportIndex52.p2 = (4, ReesGrade.Exit)`
  - `ratioCofactor52_q22 : ratioCofactor52 SupportIndex52.p2 SupportIndex52.p2 = (1, 1, 2)`
  - `SWCurrent52_q22 : SWCurrent52 SupportIndex52.p2 SupportIndex52.p2`
  - `satisfies_stage8_constraints : (E20_X52 →ₗ[ℂ] ℂ) → Prop`
  - `stage8_orientation_value_real : ∀ lam, satisfies_stage8_constraints (orientationFunctional lam) → star lam = lam`
  - `stage8_orientation_value_nonneg : ∀ lam, satisfies_stage8_constraints (orientationFunctional lam) → 0 ≤ Complex.re lam`
  - `orientationFunctional_zero_stage8 : satisfies_stage8_constraints (orientationFunctional 0)`
  - `orientationFunctional_one_stage8 : satisfies_stage8_constraints (orientationFunctional 1)`
  - `orientationFunctional_zero_ne_one : orientationFunctional 0 ≠ orientationFunctional 1`
  - `stage8_moduli_not_point : ∃ f g, satisfies_stage8_constraints f ∧ satisfies_stage8_constraints g ∧ f ≠ g`
  - `Stage8_missing_datum : Prop`
  - `J52_log_candidate_value : ℂ`
  - `J52_log_candidate : E20_X52 →ₗ[ℂ] ℂ`
  - `J52_log_candidate_value_real : star J52_log_candidate_value = J52_log_candidate_value`
  - `J52_log_candidate_value_nonneg : 0 ≤ Complex.re J52_log_candidate_value`
  - `J52_log_candidate_satisfies_stage8 : satisfies_stage8_constraints J52_log_candidate`
  - `J52_log_candidate_q22 : J52_log_candidate q22_X52 = J52_log_candidate_value`
- Proof strategy used:
  - Represent the visible `X = 5/2` support as the singleton `{2}`.
  - Define the product/Rees channel `πΣ` by `(m*n, grade m n X52)`.
  - Define the ratio/cofactor channel by `(m/g, n/g, g)` with
    `g = Nat.gcd m n`.
  - Define the SW current on the singleton support as the same-prime diagonal
    condition, which reduces to equality of support labels.
  - Prove `grade(2,2,5/2) = Exit`, so Rees interior vanishing does not apply
    to `q₂₂`.
  - Prove the primitive channel values:
    `πΣ(q₂₂) = (4, Exit)` and ratio/cofactor `(q₂₂) = (1,1,2)`.
  - Combine Gap 4 dagger compatibility with SW positivity to show any member
    of the old `orientationFunctional lam` family satisfying all Stage 8
    constraints has `star lam = lam` and `0 ≤ Complex.re lam`.
  - Exhibit two distinct valid constrained orientations, `lam = 0` and
    `lam = 1`, proving the constraints do not determine a unique
    `J_{5/2}`.
  - Test the suggested logarithmic normalization candidate
    `J_{5/2}(q₂₂) = log(2)^2`.
  - Prove this candidate is conjugation-fixed, nonnegative, and satisfies the
    full Stage 8 constraint package.
- Residual moduli:
  - The current Stage 8 data reduce the old complex freedom to nonnegative
    real magnitude, i.e. `f(q₂₂) ∈ ℝ≥0`, but do not choose a specific value.
- Missing datum:
  - A normalization or scale law for the exit product class
    `πΣ(q₂₂) = (4, Exit)` is needed to pin `J_{5/2}(q₂₂)`.
- Candidate normalization test:
  - `J_{5/2}(q₂₂) = log(2)^2` is compatible with all verified Stage 8
    constraints.
  - It is not forced by those constraints, because `0` and `1` are also
    verified distinct constrained solutions.
- Mathlib lemmas/objects used:
  - `Nat.gcd`
  - `Real.log`
  - `Complex.ofReal_re`
  - `sq_nonneg`
  - `norm_num`
  - linear map type `→ₗ[ℂ]`
  - singleton `Fintype`
- Date: 2026-09-05

## Stage 9 - Logarithmic Scale Law

- Stable IDs: SR.9A.1 through SR.9D.1, with main results
  `SR.9.MAIN` and `SR.9.DESCENT`.
- Outcome:
  - The literal all-`Nat` valuation statement is inconsistent because it
    includes `0`.
  - After correcting the domain to positive naturals, the logarithmic
    valuation law gives a positive-scale family.
  - Explicit formula compatibility pins that scale to `1`.
  - Valuation product weights are symmetric, giving the scalar content of
    antisymmetric descent.
- Theorem/object statements:
  - `ArithmeticValuation : Type`
  - `no_arithmeticValuation_nat : ¬ ∃ _v : ArithmeticValuation, True`
  - `valuation_is_log_multiple : ∀ v : ArithmeticValuation, ∃ c : ℝ, 0 < c ∧ ∀ n : Nat, 0 < n → v.val n = c * Real.log n`
  - `PosNat : Type`
  - `PositiveArithmeticValuation : Type`
  - `PositiveScale : Type`
  - `logPositiveValuation : PositiveScale → PositiveArithmeticValuation`
  - `satisfies_stage9_constraints : (E20_X52 →ₗ[ℂ] ℂ) → Prop`
  - `stage9Family : PositiveScale → (E20_X52 →ₗ[ℂ] ℂ)`
  - `stage9Family_injective : Function.Injective stage9Family`
  - `stage9_moduli : ∃ family : PositiveScale → (E20_X52 →ₗ[ℂ] ℂ), Function.Injective family ∧ ∀ c, satisfies_stage9_constraints (family c)`
  - `stage9ScaleValue_re_pos : ∀ c, 0 < Complex.re (stage9ScaleValue c)`
  - `ExplicitFormulaCompat : (E20_X52 →ₗ[ℂ] ℂ) → Prop`
  - `explicitFormula_pins_scale : ∃! c : PositiveScale, ExplicitFormulaCompat (stage9Family c)`
  - `valuation_forces_descent : ∀ v : PositiveArithmeticValuation, ∀ m n : SupportIndex6, v.val m.toPosNat * v.val n.toPosNat = v.val n.toPosNat * v.val m.toPosNat`
- Proof strategy used:
  - For the literal objective structure over all `Nat`, specialize additivity
    to `0 * 2`; this forces `val 2 = 0`, contradicting positivity at the
    verified prime `2`.
  - Introduce `PosNat` to remove the zero obstruction.
  - Construct `logPositiveValuation c` by `val n = c * Real.log n`, using
    `Real.log_mul` on positive naturals.
  - Define Stage 9 constraints as Stage 8 plus the valuation product law on
    the singleton support.
  - Prove the positive-scale family is injective by evaluating at `q₂₂` and
    cancelling the positive factor `Real.log 2`.
  - Define local explicit formula compatibility as the singleton Mangoldt
    law `J(q₂₂) = log(2)^2`.
  - Prove explicit formula compatibility has the unique scale `c = 1`.
  - Prove valuation-product descent by commutativity of real multiplication.
- Mathlib lemmas/objects used:
  - `Nat.prime_two`
  - `Nat.Prime.pos`
  - `Real.log_one`
  - `Real.log_mul`
  - `Real.log_pos`
  - `Complex.ofReal_re`
  - `nlinarith`
  - `ring`
- Date: 2026-09-05
